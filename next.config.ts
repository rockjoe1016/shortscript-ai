import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep default settings for Vercel serverless deployment.
  // If you need static export later, add: output: 'export'
};

export default nextConfig;
